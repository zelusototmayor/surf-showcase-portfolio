<section class="min-h-[70vh] py-12 bg-background">
  <div class="container mx-auto px-8">
    <div class="max-w-3xl mx-auto space-y-4">
      <% @projects.each do |project| %>
        <%= render 'project_folder', project: project %>
      <% end %>
    </div>
  </div>
</section>